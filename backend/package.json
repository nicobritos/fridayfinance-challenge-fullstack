{
  "name": "ff-transactions-backend",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/nicobritos/fridayfinance-challenge-fullstack",
  "author": "Nicolas Britos (nicbritos@hotmail.com)",
  "license": "MIT",
  "private": true,
  "scripts": {
    "compile": "tsc",
    "prisma:seed": "dotenv -e config/.env -- prisma db seed",
    "prisma:migrate": "dotenv -e config/.env -- prisma migrate dev --name init --schema ./src/prisma/schema.prisma",
    "start": "npm run compile && node ./dist/index.js"
  },
  "dependencies": {
    "@apollo/server": "^4.4.0",
    "@prisma/client": "^4.11.0",
    "graphql": "^16.6.0"
  },
  "devDependencies": {
    "@types/node": "^18.14.2",
    "dotenv-cli": "^7.0.0",
    "prisma": "^4.11.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  },
  "prisma": {
    "seed": "dotenv -e config/.env -- ts-node ./src/prisma/seed.ts"
  }
}
